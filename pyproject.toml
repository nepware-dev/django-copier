[tool.poetry]
name = "django-copier"
version = "0.1.0"
description = ""
authors = ["Saurav Sharma <saurav.sharma@nepware.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
copier = "^7.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
